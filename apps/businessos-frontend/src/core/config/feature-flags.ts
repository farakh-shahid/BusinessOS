/** UI feature toggles — admin shop settings will drive these later. */
export const featureFlags = {
  /** Order detail activity log (table + pagination when enabled). */
  activityLogEnabled: false,
} as const;
